import 'package:flutter/material.dart';

TextStyle getHeaderStyle(
    {Color fontColor = Colors.white, String fontFamily: "Audiowide"}) {
  return TextStyle(fontSize: 35.0, fontFamily: fontFamily, color: fontColor);
}
